@using Microsoft.AspNetCore.Identity
@using Project_5.Areas.Identity.Data
@using Project_5.Helpers

@inject SignInManager<Project_5User> SignInManager
@inject UserManager<Project_5User> UserManager

@model IEnumerable<Aspnetuser>
@using static Microsoft.AspNetCore.Identity.IdentityRole
@{
    ViewData["Title"] = "Student Management";
}

<h1>Your Students</h1>

<table class="table table-sm table-bordered">
    <tr><th>Name</th><th>Email</th><th></th></tr>
    @foreach (Aspnetuser user in Model)
    {
        {
            <tr>
                <td>@user.UserName</td>
                <td>@user.Email</td>
                <td><a href="@("/faculty/" + user.Id)"><button type="button">View Plans</button></a></td>
            </tr>
        }
    }
</table>
